# Served behind Cloudflare Tunnel (TLS terminates at the edge), so plain :80.
server {
    listen       80;
    server_name  _;
    root         /usr/share/nginx/html;
    index        index.html;

    # TLS terminates at Cloudflare, so absolute redirects would be built as
    # http:// and cost the client an extra edge round-trip. Relative keeps the
    # /impressum -> /impressum/ hop on https in one step.
    absolute_redirect off;

    # Real client IP from the tunnel, for logs only.
    set_real_ip_from 0.0.0.0/0;
    real_ip_header CF-Connecting-IP;

    gzip on;
    gzip_types text/css application/javascript image/svg+xml application/json;
    gzip_min_length 1024;

    # Hashed Astro assets are immutable — cache hard.
    location /_astro/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    # No secrets here, but keep the basics tidy.
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    server_tokens off;
}
