// Where the marketing CTAs point. The landing owns the site root and the app
// is served from /app/ on the same origin now; ?start=1 mints a guest session
// on arrival (returning users just land in their library).
export const APP_URL = '/app/?start=1';
export const REPO = 'https://github.com/one-more-refactor/flick';
export const SITE = 'https://myflick.app';
